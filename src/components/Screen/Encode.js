import * as React from "react";
import * as classess from "@styles/module/encode.module.css";

const Encode = () =>{

    return(
        <article className={classess.summary}>
            <div className={classess.left}>
                <img src={} alt="steganography" />
            </div>
            <div className={classess.right}>

            </div>
        </article>
    );
}

export default Encode;